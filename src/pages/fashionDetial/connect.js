
export const mapStateToProps = (state)=>({
    productId:state.fashion.productId,
    goods_gallery:state.fashion.goods_gallery,
    a:state.fashion.a,
    installment_info:state.fashion.installment_info
})


export const mapDispatchToProps = (dispatch)=>({
    
})